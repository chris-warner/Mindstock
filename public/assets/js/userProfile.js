var conservitive; 
var moderate;
var aggressive;

var conservitive; 
var moderate;
var aggressive;


function calculateRisk(fromProfile)
{
    function getProfile()
    {
        if ( fromProfile()
        {
            
    }   
    let risk = result; 
    return risk;
}


// "There is a definition of risk by a formula: "risk = probability x loss".What does it mean ? 
// Many authors refer to risk as the probability of loss multiplied by the amount of loss(in monetary terms ).
// Is this right,or at least acceptable,as a definition of risk ? "